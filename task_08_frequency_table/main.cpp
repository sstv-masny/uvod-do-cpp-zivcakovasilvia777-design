
#include <iostream>
#include <vector>

using namespace std;

int main() {
std:
    ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int n;
    if (!std::cin >> n)
        return 0;
    return 0;
}